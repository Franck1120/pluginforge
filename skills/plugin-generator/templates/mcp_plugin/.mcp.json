{
  "mcpServers": {
    "{{PLUGIN_NAME}}-server": {
      "command": "python",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp-server/server.py"
      ],
      "env": {
        "PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
